# phpDevTemplate

After clone this, creating folder **/php-lab-mysql-data** for mysql data binding. If don't use this folder and want to use another, remember to edit docker-compose file as well.

# Using Nginx

In this case, the nginx.conf is used as Web server for PHP.
